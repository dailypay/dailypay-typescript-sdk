/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { SDKCore } from "../core.js";
import { SDKOptions } from "../lib/config.js";
import type { ConsoleLogger } from "./console-logger.js";
import { createRegisterPrompt } from "./prompts.js";
import {
  createRegisterResource,
  createRegisterResourceTemplate,
} from "./resources.js";
import { MCPScope } from "./scopes.js";
import { createRegisterTool } from "./tools.js";
import { tool$accountsCreate } from "./tools/accountsCreate.js";
import { tool$accountsList } from "./tools/accountsList.js";
import { tool$accountsRead } from "./tools/accountsRead.js";
import { tool$authenticationRequestToken } from "./tools/authenticationRequestToken.js";
import { tool$cardsCreate } from "./tools/cardsCreate.js";
import { tool$healthGetHealth } from "./tools/healthGetHealth.js";
import { tool$jobsList } from "./tools/jobsList.js";
import { tool$jobsRead } from "./tools/jobsRead.js";
import { tool$jobsUpdate } from "./tools/jobsUpdate.js";
import { tool$organizationsList } from "./tools/organizationsList.js";
import { tool$organizationsRead } from "./tools/organizationsRead.js";
import { tool$paychecksList } from "./tools/paychecksList.js";
import { tool$paychecksRead } from "./tools/paychecksRead.js";
import { tool$peopleRead } from "./tools/peopleRead.js";
import { tool$peopleUpdate } from "./tools/peopleUpdate.js";
import { tool$transfersCreate } from "./tools/transfersCreate.js";
import { tool$transfersList } from "./tools/transfersList.js";
import { tool$transfersRead } from "./tools/transfersRead.js";

export function createMCPServer(deps: {
  logger: ConsoleLogger;
  allowedTools?: string[] | undefined;
  scopes?: MCPScope[] | undefined;
  serverURL?: string | undefined;
  security?: SDKOptions["security"] | undefined;
  version?: SDKOptions["version"] | undefined;
  serverIdx?: SDKOptions["serverIdx"] | undefined;
  environment?: SDKOptions["environment"] | undefined;
}) {
  const server = new McpServer({
    name: "SDK",
    version: "0.0.1",
  });

  const client = new SDKCore({
    security: deps.security,
    version: deps.version,
    serverURL: deps.serverURL,
    serverIdx: deps.serverIdx,
    environment: deps.environment,
  });

  const scopes = new Set(deps.scopes);

  const allowedTools = deps.allowedTools && new Set(deps.allowedTools);
  const tool = createRegisterTool(
    deps.logger,
    server,
    client,
    scopes,
    allowedTools,
  );
  const resource = createRegisterResource(deps.logger, server, client, scopes);
  const resourceTemplate = createRegisterResourceTemplate(
    deps.logger,
    server,
    client,
    scopes,
  );
  const prompt = createRegisterPrompt(deps.logger, server, client, scopes);
  const register = { tool, resource, resourceTemplate, prompt };
  void register; // suppress unused warnings

  tool(tool$authenticationRequestToken);
  tool(tool$jobsRead);
  tool(tool$jobsUpdate);
  tool(tool$jobsList);
  tool(tool$accountsRead);
  tool(tool$accountsList);
  tool(tool$accountsCreate);
  tool(tool$transfersRead);
  tool(tool$transfersList);
  tool(tool$transfersCreate);
  tool(tool$paychecksRead);
  tool(tool$paychecksList);
  tool(tool$organizationsRead);
  tool(tool$organizationsList);
  tool(tool$peopleRead);
  tool(tool$peopleUpdate);
  tool(tool$cardsCreate);
  tool(tool$healthGetHealth);

  return server;
}
