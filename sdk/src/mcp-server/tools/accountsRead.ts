/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsRead } from "../../funcs/accountsRead.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ReadAccountRequest$inboundSchema,
};

export const tool$accountsRead: ToolDefinition<typeof args> = {
  name: "accounts-read",
  description: `Get an Account object

Returns details about an account. This object represents a person's bank accounts, debit and pay cards, and earnings balance accounts.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountsRead(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
