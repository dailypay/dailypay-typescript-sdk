/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsRead } from "../../funcs/organizationsRead.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ReadOrganizationRequest$inboundSchema,
};

export const tool$organizationsRead: ToolDefinition<typeof args> = {
  name: "organizations-read",
  description: `Get an organization

Lookup organization by ID for a detailed view of single organization.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsRead(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
