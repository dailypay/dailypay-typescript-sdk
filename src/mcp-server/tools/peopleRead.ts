/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { peopleRead } from "../../funcs/peopleRead.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ReadPersonRequest$inboundSchema,
};

export const tool$peopleRead: ToolDefinition<typeof args> = {
  name: "people-read",
  description: `Get a person object

Returns details about a person.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await peopleRead(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
